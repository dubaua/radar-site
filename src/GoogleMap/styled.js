import styled from "styled-components";

export const Wrapper = styled.div`
  position: fixed;
  top: 139px;
  bottom: 0;
  background: #eee;
  z-index: 1;
  width: calc(50vw - (992px - 16px) /6 - 13px);
`;
